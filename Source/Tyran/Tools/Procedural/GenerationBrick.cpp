// Fill out your copyright notice in the Description page of Project Settings.

#include "GenerationBrick.h"

/*
void GenerationBrick::addTransformation(Transformation transform, int score)
{
	availableTransformations.Add(transform);
	transformationScore.Add(score);
	scoreSum += score;
}

GenerationBrick::Transformation GenerationBrick::getTransformation()
{
	int score = FMath::RandRange(0, scoreSum);
	int tempSum = 0;
	for (int i = 0; i < availableTransformations.Num(); ++i) {
		tempSum += transformationScore[i];
		if (score <= tempSum) {
			return availableTransformations[i];
		}
	}
	return availableTransformations[0];
}

bool GenerationBrick::isTransformable()
{
	return availableTransformations.Num() == 0;
}

GenerationBrick::GenerationBrick()
{
	scoreSum = 0;
}

GenerationBrick::~GenerationBrick()
{
}*/

